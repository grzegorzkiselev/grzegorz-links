var fontSize = 240,
  lineHeight = 1.2,
  unitsPerEm = 1e3,
  ascent = 775,
  descent = -255,
  capHeight = 708,
  xHeight = 491;
const y = lineHeight,
  x = d / g,
  w = f / g,
  v = p / g,
  O = b / g,
  H = -1 * O,
  m = (p + Math.abs(b)) / g,
  S = ((y - m) / 2) * a,
  q = S + H * a,
  R = q + v * a,
  j = q + x * a,
  k = q + w * a,
  z = q + O * a;
